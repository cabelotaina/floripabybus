<%= form_for(@bustime) do |f| %>
  <% if @bustime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bustime.errors.count, "error") %> prohibited this bustime from being saved:</h2>

      <ul>
      <% @bustime.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :time %><br>
    <%= f.time_select :time %>
  </div>
  <div class="field">
    <%= f.label :label %><br>
    <%= f.text_field :label %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
